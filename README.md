# 🍅⚔️ PomoQuest — Pomodoro RPG

โฟกัสงาน… แล้วตัวละครของคุณจะผจญภัยไปกับคุณ!

PomoQuest ผสม **เทคนิค Pomodoro** เข้ากับ **เกม RPG** — ทุกครั้งที่คุณตั้งใจโฟกัสงาน ตัวละครของคุณจะออกเดินทางผจญภัย เก็บเลเวล ต่อสู้กับมอนสเตอร์ หาสมบัติ สู้กับบอส และเดินทางข้ามเมืองไปเรื่อย ๆ

## 🎮 แนวคิดการเล่น

| ช่วงเวลา | สิ่งที่เกิดขึ้นในเกม |
|---|---|
| **Work session (25 นาที)** | ⚔️ ผจญภัยในเมืองปัจจุบัน — ทุก 30–90 วินาที (สุ่ม) จะเกิดเหตุการณ์สุ่ม: เจอมอนสเตอร์ (สู้แบบอัตโนมัติ ไม่รบกวนสมาธิ) เจอสมบัติ ศาลเจ้าลึกลับ พ่อค้าเร่ร่อน หรือกับดัก — **ระหว่างโฟกัสไม่เด้ง modal** (ไม่บล็อกหน้าจอ) เห็นแค่ toast สรุปรางวัล (แบบคิว — โชว์เรียงกัน ไม่ทับกัน) + log เหตุการณ์ย้อนหลัง (เรียงล่าสุดบนสุด พร้อมเวลา) และ **สรุป session** รวมของรางวัลทุกเหตุการณ์ตอนจบ session |
| **พักสั้น (5 นาที)** | 🔥 ค่ายพัก — ซื้อของที่ร้านค้า ทำภารกิจย่อย (สำเร็จ/เสี่ยง) ใช้ไอเทม ฟื้นพลังฟรี หรือจัดสรรแต้มสถานะ |
| **ครบ 4 sessions** | 👹 **พักยาว (15 นาที) = สู้บอส!** ต่อสู้แบบเทิร์น-by-เทิร์น (โจมตี/สกิล/ใช้ยา) ชนะแล้วได้รางวัลใหญ่และเดินทางสู่เมืองถัดไป |

### ระบบหลัก
- 🧑‍🎤 **สร้างตัวละครหลายตัว** — เลือกชื่อ + 1 ใน 4 คลาส (นักรบ / นักเวทย์ / โจร / นักบวช) สร้างได้หลายตัว สลับเล่นได้ เปลี่ยนชื่อได้ (ห้ามชื่อซ้ำ) ลบได้ (ปุ่ม 👥 + ยืนยัน 2 ครั้งกันลบพลาด) แต่ละตัวมี progress/ไอเทม/timer แยกกัน
- 📈 **เลเวล & แต้มสถานะ** — สะสม XP เพื่อเลเวลอัพ ได้แต้มจัดสรรเอง (HP / MP / ATK / DEF / SPD) พร้อมการเติบโตอัตโนมัติ
- 🎒 **ไอเทม & อุปกรณ์ (RPG)** — ระบบช่องสวมใส่ 11 ช่อง: อาวุธมือหลัก/มือรอง (มือรองใส่ได้ทั้งอาวุธมือเดียวหรือโล่ — อาวุธสองมือจะปิดช่องมือรอง), หมวก (หัว), เกราะ (ตัว), แขน, ขา, เท้า และเครื่องประดับ 4 ช่อง (ซื้อ/ขาย/สวม/ถอด/ใช้) — อุปกรณ์มี**ข้อจำกัดการสวม**: บางชิ้นเฉพาะคลาส (ป้าย 🎭 สีประจำคลาส: แดง=นักรบ, ฟ้า=นักเวทย์, เทา=โจร, เขียว=นักบวช), ต้องเลเวลขั้นต่ำ (🔒เลเวล X+), หรือต้องมีค่าสถานะถึงเกณฑ์ (🔒 ATK/DEF/SPD/MP X+) — ดูได้จากชิปที่ไอเทม ปุ่มสวมจะปิดพร้อมบอกเหตุผล (ร้านค้า/ดรอป/รางวัล Daily Quest กรองของเฉพาะคลาสอื่นออกให้อัตโนมัติ) · **ล่าได้ของ**: ชนะมอนสเตอร์ ~40% ได้ของขวัญประจำตัว (ฟันหนูยักษ์/ปีกค้างคาว/เกล็ดมังกรน้อย ฯลฯ — ราคาต่ำ) · **ชนะบอส ~50% ได้ของรางวัลประจำตัว** (ตราโจรป่า/หัวใจจอมมาร ฯลฯ — ขายแพงกว่า) — ทั้งหมดนับรวมในเควสประจำวัน "คนเก็บขยะ"
- 📖 **คู่มือล่า** (แท็บสถิติ) — ตารางมอนสเตอร์ 8 ตัว + บอส 8 ตัว พร้อมของที่ดรอป (ไอคอน/ชื่อ/ราคา) ให้วางแผนล่าของได้
- 🖤 **ตลาดมืด** — เจอสุ่ม ~25% ต่อค่ายพัก (พ่อค้าเงาลึกลับ): **รับซื้อของขวัญ (junk) แพงกว่าปกติ +25%** และขายของ 4 ชิ้นราคาลดพิเศษ — คัมภีร์สกิล (ลด 15%), ของหายาก (ลด 25%), ของเถื่อนเก็งกำไร (ลด 45% — เก็บไว้ขายค่ายหน้าที่พ่อค้าต้องการ), **ของพิเศษ exclusive ที่หลุดมาจาก Daily Quest** (ลด 10% — ยกเว้นถุงเงินนำโชค กันวนซื้อแล้วใช้ +150 ทอง) — ซื้อได้ครั้งเดียวต่อค่ายพัก หน้าเดิมเปิดซ้ำ ของ/ราคาเหมือนเดิม
- 🆕 **วิธีเล่นสำหรับคนใหม่** — ครั้งแรกที่เข้าหน้าแรกจะเด้ง modal สรุปการเล่น 5 ขั้นตอน (โฟกัส → session → ครบ 4 สู้บอส → สวมอุปกรณ์ → ตั้งค่า) ปิดแล้วไม่โผล่อีก
- 🔥 **คอมโบโฟกัส** — ทำ session ต่อเนื่องไม่ทิ้ง ได้โบนัส XP สูงสุด x1.5
- 🗺️ **8 เมือง** — ชนะบอสทีละเมืองเพื่อเดินทางต่อไป (บอสและมอนสเตอร์แข็งแกร่งขึ้นตามเลเวล) และ **เดินทางย้อนกลับ** ไปเมืองที่เคยไปมาแล้วได้ (เสีย 20 ทอง/เมือง — ไปข้างหน้าต้องชนะบอสเท่านั้น)
- 📅 **Daily Quest (ภารกิจประจำวัน)** — สุ่ม 3 ภารกิจต่อวัน (รีเซ็ตเที่ยงคืน) เช่น โฟกัสครบ 2 session, กำจัดมอนสเตอร์ 5 ตัว, ชนะบอส 1 ตัว — ทำเสร็จแล้ว**เลือกรางวัลเองได้** (ทอง / XP / ไอเทมสุ่ม) และทำครบทุกภารกิจได้โบนัสพิเศษ (ทอง + XP + ไอเทม) พร้อม **streak ภารกิจ**: ทำครบติดต่อกันหลายวัน โบนัสเพิ่ม 20%/วัน สูงสุด x2
  - **ไอเทมพิเศษ (✦ พิเศษ)** — ของ Exclusive 5 อย่างที่หาซื้อตามร้านไม่ได้ (ถุงเงินนำโชค, คัมภีร์ประสบการณ์, สมุดนำโชค, มงกุฎนักโฟกัส, อีลิกเซอร์บริสุทธิ์) ได้จากการเลือกรางวัล "ไอเทมสุ่ม" (40%) หรือโบนัสครบทุกภารกิจ (35%) เท่านั้น
- 🏅 **ระบบ Achievement (41 ตรา: ปกติ 26 + ลับ 15)** — ปลดล็อกอัตโนมัติเมื่อถึงเงื่อนไข ได้รางวัลทอง/XP ดูความคืบหน้าได้ที่แท็บ "ตรา" — รวมตราใหม่ **🎭 เต็มยศประจำคลาส**: สวมอุปกรณ์เฉพาะคลาส 2 ชิ้นพร้อมกัน
  - **ตราปกติ** — session, บอส, มอนสเตอร์, สมบัติ, ภารกิจ, คอมโบ, เลเวล, ทอง ฯลฯ (โชว์ความคืบหน้า)
  - **ตราลับ** — เงื่อนไขซ่อนไว้ (โชว์เป็น "???" พร้อมคำใบ้ลึกลับ เช่น โฟกัสตอนตี 2, ชนะบอสตอน HP=1, สู้บอสโดยไม่สวมอุปกรณ์, พิชิตครบ 8 เมือง) รางวัลสูงกว่า
- 📊 **หน้า Stats** — **สรุปรายสัปดาห์** (session/เวลา/XP/ทอง/บอส/มอนสเตอร์/เมือง/ตรา เทียบสัปดาห์ก่อน พร้อม ↑↓%) + กราฟโฟกัสย้อนหลัง 7 วัน + กราฟจำนวน session ต่อวัน (7 วัน + 30 วัน — **สลับดูเป็นเวลาโฟกัสได้**) + เวลาโฟกัสสะสม + สถิติต่อสู้ + เส้นทางการเดินทางข้ามเมือง
- 📜 **บันทึกการผจญภัย** — ดูประวัติทุกเหตุการณ์ รางวัล (รวมพลังที่เสีย/ฟื้นจากเหตุการณ์) และสถิติสะสม — ทุก session จะมีรายการสรุปกำกับ label เช่น `📋 Session 2/4 @ 21:09` (สรุปชนิดเหตุการณ์ + ของรางวัลรวม) เก็บ 1 แถวต่อ session ดูย้อนหลังได้เรื่อย ๆ
- 📅 **ประวัติ Session** (แท็บใหม่) — ดู session ย้อนหลังเรียงล่าสุดก่อน **กรองได้ 3 แบบ: วันที่ / เมือง (dropdown เฉพาะเมืองที่เคยผ่าน + เมืองปัจจุบัน) / คำค้น** กดขยายดูเหตุการณ์ทั้งหมดใน session นั้นได้ (ชนิด/รายละเอียด/ของรางวัล/เวลา) — session log ระหว่างโฟกัสก็แสดงรายละเอียดเหตุการณ์ (เช่น เจอมอนสเตอร์ตัวไหน ชนะ/หนี ได้อะไร) เป็นบรรทัดเพิ่มใต้ชื่อเหตุการณ์
- 📱 **PWA** — ติดตั้งเป็นแอพบนมือถือได้ (Add to Home Screen) ใช้งานแบบ standalone
- 🔔 **แจ้งเตือนเบราว์เซอร์** — เด้ง notification ของระบบตอนจบ session / พักหมด / ถึงเวลาสู้บอส (ขออนุญาตครั้งแรกตอนเริ่มโฟกัส — แจ้งเฉพาะตอนแท็บอยู่เบื้องหลัง, กดแล้วกลับเข้าเกมได้, เปิด/ปิดได้ในแท็บตั้งค่า + โชว์สถานะการอนุญาต)

## 🛠️ Tech Stack

- **Frontend:** React 19 + Vite (mobile-first CSS, ไม่มี UI framework)
- **Backend:** Express
- **Database:** SQLite (better-sqlite3) — เก็บที่ `server/data/pomoquest.db`
- **PWA:** manifest + service worker (network-first) + ไอคอน PNG สร้างจาก pixel art

## 🚀 วิธีรัน

**รันง่ายด้วย script** (ดูวิธีใช้เต็ม: `./run.sh help`):

```bash
./run.sh            # โหมดพัฒนา (server :3001 + frontend :5173)
./run.sh prod       # build + รัน production ที่ http://localhost:3001
./run.sh start      # รัน production แบบ background (log: /tmp/pomoquest.log)
./run.sh stop       # หยุด server
./run.sh reset      # RESET เกม: หยุด server → ลบฐานข้อมูล → รันใหม่ (พิมพ์ reset ยืนยัน, -y ข้าม)
./run.sh backup     # สำรองฐานข้อมูลไปที่ backups/ (snapshot สม่ำเสมอ — server รันอยู่ได้)
./run.sh backup --json   # สำรองเป็น .json.gz (บีบอัด, อ่าน/แก้ได้ — import มีผลทันที)
./run.sh restore    # กู้คืนจาก backup (.db หรือ .json.gz) — พิมพ์ restore ยืนยัน (เช็คเวอร์ชัน schema ก่อน)
./run.sh status     # ดูสถานะ server + LLM
./run.sh llm        # เช็คว่า LLM ที่ localhost:8080 พร้อมใช้ไหม
```

หรือรันด้วย npm ตรง ๆ:

```bash
npm install          # ติดตั้ง dependencies (ครั้งแรก)
npm run dev          # โหมดพัฒนา — server :3001 + frontend :5173 (เปิด http://localhost:5173)
```

โหมด production:

```bash
npm run build        # build frontend ไปที่ dist/
npm start            # รัน server เดียว เสิร์ฟทั้งแอพที่ http://localhost:3001
```

> 💡 บนมือถือ: เปิดเว็บใน Chrome → เมนู → **Add to Home Screen** → ใช้งานเหมือนแอพจริง

## 📁 โครงสร้างโปรเจกต์

```
├── server/
│   ├── index.js      # Express entry (เสิร์ฟ dist ใน production)
│   ├── routes.js     # API ทั้งหมด
│   ├── db.js         # SQLite schema + seed
│   ├── game.js       # game engine (XP, ต่อสู้, event, บอส, quest)
│   ├── data.js       # ข้อมูลเกม (คลาส, ไอเทม, เมือง, มอนสเตอร์, เหตุการณ์)
│   └── data/         # ไฟล์ฐานข้อมูล (auto-create, อย่า commit)
├── src/
│   ├── Game.jsx      # phase machine: idle → work → พักสั้น → บอส
│   ├── context.jsx   # state กลาง + API actions + toast + level-up signal
│   └── components/   # UI ทั้งหมด (creation, timer, camp, boss, ฯลฯ)
├── public/           # PWA (manifest, sw.js, ไอคอน)
└── scripts/          # gen-icons.mjs — สร้างไอคอนจาก pixel art
```

## ➕ เพิ่มเนื้อหาเกมเอง (มอนสเตอร์ / อุปกรณ์ / เมือง)

ข้อมูลเกมทั้งหมดอยู่ในไฟล์เดียว: **`server/data.js`** — เกม engine อ่านจาก array เหล่านี้โดยตรง ดังนั้น **เพิ่มเนื้อหาใหม่ไม่ต้องแตะโค้ดอื่นเลย** (ไม่ต้องแก้ engine, API หรือ UI) เปิดแอพใหม่แล้วใช้งานได้ทันที

### 🐾 เพิ่มมอนสเตอร์

เพิ่ม entry ใน `MONSTERS` array:

```js
{ name: 'กอร์กอน', icon: '🐍', power: 1.4, xp: 48, gold: 30, loot: 120 },
```

| field | ความหมาย |
|---|---|
| `name` / `icon` | ชื่อและอีโมจิแสดงผล |
| `power` | ตัวคูณพลังตามเลเวลตัวละคร (0.7 = อ่อน, 1.5 = แรง) — ยิ่งสูงยิ่งชนะยาก |
| `loot` | **(ไม่บังคับ)** id ของไอเทมขยะประจำตัวมอนสเตอร์ (ดูใน `ITEMS`) — **ชนะมอนสเตอร์มีโอกาส ~40%** ได้ของนี้ (ขยะราคาต่ำ ขายได้ที่แคมป์) — ถ้าไม่ระบุ ไม่มี loot |
| `xp` / `gold` | รางวัลพื้นฐานตอนชนะ (ได้เพิ่มตามเลเวลอัตโนมัติ) |

### ⚔️ เพิ่มอุปกรณ์ / ไอเทม

เพิ่ม entry ใน `ITEMS` array — ช่องสวมใส่ถูกเลือกอัตโนมัติจาก `type` (11 ช่อง: มือหลัก/มือรอง/หมวก/เกราะ/แขน/ขา/เท้า/เครื่องประดับ x4):

```js
// อาวุธ (handed: 1 = มือเดียว → ใส่มือหลักหรือมือรอง, 2 = สองมือ → มือหลัก + ปิดมือรอง)
{ id: 14, name: 'ดาบเพลิง', icon: '🔥', type: 'weapon', atk_bonus: 12, price: 260, desc: 'ดาบที่ร้อนแรง', lvl: 4, handed: 1 },

// เกราะ/หมวก/แขน/ขา/เท้า/โล่/เครื่องประดับ (ใส่ช่องตาม type)
{ id: 50, name: 'โล่ไม้', icon: '🛡️', type: 'shield', def_bonus: 4, hp_bonus: 8, price: 45, desc: 'โล่ไม้เก่า ๆ', lvl: 1 },

// ของใช้ (กิน/ใช้แล้วหาย) — heal_pct / mana_pct / use_gold / use_xp
{ id: 45, name: 'ยามหัศจรรย์', icon: '🍯', type: 'consumable', heal_pct: 0.5, mana_pct: 0.3, price: 90, desc: 'ฟื้น HP 50% และ MP 30%' },
```

| field | ความหมาย |
|---|---|
| `id` | **ต้องไม่ซ้ำกัน** (ระบบใช้ id เป็น key) — ตอนนี้ใช้ 1-4 ยา, 5-7 สมุนไพร, 10-17 อาวุธ, 20-23 เกราะตัว, 30-37 เครื่องประดับ, 40-44 exclusive, 45-49 junk, 50-52 โล่, 60-62 หมวก, 70-72 แขน, 80-82 ขา, 90-92 เท้า, **200-207 ไอเทมเฉพาะคลาส** (ขวานมังกรเพลิง/เกราะไททัน=นักรบ, คทาแสงจันทร์/เสื้อคลุมเวทมนตร์=นักเวทย์, มีดอาบพิษ/ชุดเงา=โจร, ค้อนศักดิ์สิทธิ์/ชุดนักบวช=นักบวช) |
| `type` | `weapon` / `shield` / `armor` (ตัว) / `head` (หมวก) / `arms` / `legs` / `feet` / `accessory` (เครื่องประดับ) หรือ `consumable` (ของใช้) |
| `handed` | **(เฉพาะ weapon)** `1` = มือเดียว (มือรองใส่ได้), `2` = สองมือ (มือรองถูกปิดอัตโนมัติ) |
| โบนัส | ตามประเภท: `atk_bonus` `def_bonus` `hp_bonus` `mp_bonus` `spd_bonus` `crit_bonus` (อุปกรณ์), `heal_pct` `mana_pct` `use_gold` `use_xp` (ของใช้) |
| `lvl` | ระดับขั้นต่ำที่สวม/ดรอปได้ (อุปกรณ์) — ดรอปต้อง `lvl ≤ เลเวลตัวละคร + 1` |
| `classReq` | **(ไม่บังคับ)** array ของคลาสที่สวมได้ เช่น `['warrior']` `['mage','cleric']` — คลาสอื่นสวมไม่ได้ (ร้านค้า/ดรอป/รางวัล Daily จะกรองของคลาสอื่นออกให้อัตโนมัติ) |
| `statReq` | **(ไม่บังคับ)** ค่าสถานะขั้นต่ำ (รวมอุปกรณ์ที่สวมอยู่) เช่น `{ atk: 20 }` `{ def: 12, spd: 16 }` — ไม่ถึงเกณฑ์สวมไม่ได้ (คีย์: `atk` `def` `spd` `mp` `crit`) |
| `price` / `desc` | ราคาขายในร้านและคำอธิบาย |
| `exclusive: true` | **(ไม่บังคับ)** ตั้งไว้ = ไอเทมพิเศษเฉพาะ Daily Quest — **ร้านค้าไม่ขาย** และไม่ดรอปตามปกติ (ได้จากการเลือกรางวัล "ไอเทมสุ่ม" / โบนัสครบภารกิจเท่านั้น) |

### ⚠️ ข้อควรรู้

- **ร้านค้า:** ขายอุปกรณ์ทุกชิ้นที่ `ไม่ใช่ exclusive` อัตโนมัติ แต่ **ของใช้ (consumable) ขายเฉพาะ id 1-4** — ถ้าอยากขายยาตัวใหม่ในร้าน ต้องเพิ่ม id ในเงื่อนไข `SHOP_STOCK` (บรรทัดเดียวกับนิยาม `SHOP_STOCK` ใน `data.js`)
- **การดรอป:** เกมสุ่มดรอปตามระดับ — อุปกรณ์ `lvl` สูงเกินเลเวลตัวละครจะยังไม่ดรอป (ตั้งใจให้เป็นของหายาก)
- **exclusive:** ไอเทม `exclusive: true` จะถูกกรองออกจากร้านและดรอปทั้งหมดอัตโนมัติ — ใช้สำหรับของ Daily Quest เท่านั้น
- **เมือง & บอส:** เพิ่มได้เหมือนกัน (เพิ่ม `CITIES` + `BOSSES` คู่กัน จำนวนต้องเท่ากัน เพราะบอสผูก index กับเมือง) — บอสรับ field `loot` (id ไอเทมขยะ) ชนะแล้วดรอป ~50% — แต่ระวังตราลับ "ผู้พิชิตแอสการ์ด" ที่ผูกกับ index เมืองที่ 8 (แก้ใน `data.js` → `SECRET_ACHIEVEMENTS` ได้)

## 🤖 ระบบ LLM (เนื้อเรื่องสุ่มเสริม)

เกมสามารถเชื่อม **LLM** เพื่อเขียน "เรื่องราวการผจญภัย" สั้น ๆ ลงในบันทึกหลังจบแต่ละ session (📖 เรื่องราวการผจญภัย) — เนื้อเรื่องไม่ซ้ำเดิมทุกครั้ง ภาษาไทยปนอังกฤษตามสไตล์เกม

> ⚠️ LLM เขียนแค่ **ข้อความบรรยายเท่านั้น** — XP/ทอง/ไอเทม/รางวัลทั้งหมดยังคำนวณที่ server ตามเดิม (กันโกง + กัน balance พัง) และ **ถ้าไม่มี key หรือ API error เกมจะใช้ข้อความตายตัวเดิมปกติ** ไม่พัง

### วิธีเปิดใช้ — ค่า default ชี้ไป LLM ในเครื่อง (`localhost:8080/v1`)

**รันตามปกติได้เลย ไม่ต้องตั้ง env อะไร** — โมดูลจะเรียก `http://localhost:8080/v1/chat/completions` ด้วย `model: "default"` โดยอัตโนมัติ (กรณี base URL เป็น localhost เกมจะเปิดใช้ LLM เอง ไม่ต้อง auth):

```bash
npm run dev
```

> ตัวอย่าง: รัน g4f instance / Ollama / LLM server ใดก็ได้ที่ OpenAI-compatible ไว้ที่ port 8080 (เช่น `g4f` local: `python -m g4f`)

ตั้งค่าเพิ่มเติม (ไม่บังคับ):

```bash
LLM_BASE_URL=http://localhost:8080/v1   # base URL ของ API (ค่า default)
LLM_MODEL=default            # ชื่อโมเดล — ค่า default คือ "default"
LLM_TIMEOUT_MS=30000         # timeout ต่อ request (เผื่อ cold start โมเดล local)
LLM_ENABLED=0                # บังคับปิด LLM (ไม่เรียกเลย)
```

### ใช้กับ g4f.space (remote — ต้อง auth)

- **g4f.space ตรวจ credits ฝั่ง backend** — ต่อให้ใช้ `g4f.dev/dist/js/client.js` ใน browser ตรง ๆ ก็เจอ error `402 No cake credits` ถ้าเครื่องยังไม่มี credits (ทดสอบจริงแล้ว)
- วิธีใช้: bake proof-of-work ฟรีที่ `g4f.dev/chat` → credits ผูกกับ IP → รันด้วย `LLM_ENABLED=1 npm run dev` (ไม่ต้อง key) หรือสมัคร `g4f.dev/members.html` เอา key มาใส่ `LLM_API_KEY`

```bash
LLM_ENABLED=1 npm run dev          # credits ผูก IP (bake ฟรีแล้ว)
LLM_API_KEY=xxxx npm run dev       # มี key
```

### จุดที่ LLM ถูกใช้ตอนนี้

| จุด | ผลลัพธ์ |
|---|---|
| หลังจบ work session (`/adventure/complete`) | เขียนเรื่องราว 2-3 ประโยคจากบริบท (ตัวละคร/เมือง/เวลาที่โฟกัส/คอมโบ) → บันทึกเป็น log "📖 เรื่องราวการผจญภัย" |

## 🔌 API หลัก

| Method | Path | ความหมาย |
|---|---|---|
| GET | `/api/state` | สถานะรวม (ตัวละคร, สถิติ, กระเป๋า, บันทึก, ตั้งค่า) |
| POST | `/api/character/create` | สร้างตัวละคร (หลายตัวได้) |
| POST | `/api/character/select` · `/rename` · `/delete` | เลือก / เปลี่ยนชื่อ / ลบตัวละคร |
| GET | `/api/characters` | รายชื่อตัวละครทั้งหมด |
| GET | `/api/stats` | สถิติละเอียด (กราฟ 7 วัน + สถิติต่อสู้) |
| GET | `/api/session-history` | ประวัติ session — สรุป + เหตุการณ์ทั้งหมดในแต่ละ session (จับกลุ่มด้วย session_key) |
| GET | `/api/weekly-summary` | สรุปรายสัปดาห์ — 7 วันล่าสุด เทียบ 7 วันก่อนหน้า (session/เวลา/XP/ทอง/บอส/เมือง/ตรา) |
| POST | `/api/adventure/event` | สุ่มเหตุการณ์ระหว่าง work session |
| POST | `/api/adventure/complete` | จบ session → แจกรางวัล + คอมโบ |
| GET | `/api/camp` | ร้านค้า + ภารกิจ (ช่วงพัก) |
| POST | `/api/shop/buy` · `/api/quest/do` | ซื้อของ / ทำภารกิจ |
| GET/POST | `/api/boss` · `/api/boss/act` | สู้บอส (โจมตี/สกิล/ยา) |
| POST | `/api/character/allocate` | จัดสรรแต้มสถานะ |
| POST | `/api/travel` | เดินทางย้อนกลับไปเมืองที่เคยไปมาแล้ว (เสีย 20 ทอง/เมือง) |
| PUT | `/api/settings` | ปรับเวลา Pomodoro |
| GET | `/api/backup` | Export ข้อมูลทั้งหมดเป็นไฟล์ .db (ดาวน์โหลด) |
| GET | `/api/export` | Export เป็น JSON บีบอัด gzip (.json.gz — อ่าน/แก้ได้ ไฟล์เล็ก) |
| POST | `/api/restore` | Import — .json.gz มีผลทันที · .db ต้องรีสตาร์ท server — ทั้งคู่เช็คเวอร์ชัน schema ก่อน (ไฟล์เวอร์ชันเก่าถูก reject พร้อมข้อความ) |
| POST | `/api/reset` | ล้างข้อมูลเกมทั้งหมด (มีผลทันที) |

## 🧪 Dev Test Panel

เข้าได้จากหน้าแรก (ปุ่ม dev) — **เข้าสู่ระบบด้วย `admin/admin`** (ตั้งค่าเองได้ผ่าน env `DEV_USER` / `DEV_PASS`) มีปุ่มทางลัดเข้าสู่ระบบแบบคลิกเดียว

ทดสอบได้ทั้ง: สุ่มเหตุการณ์ระหว่าง session (บังคับ event ตามชนิดได้), จบ session / พักเบรก, ภารกิจสุ่ม, ซื้อของ, ชนะบอสทันที, เติม HP/MP เต็ม, ย้ายเมืองถัดไป, เรื่องราวทดสอบ, เรียนรู้สกิล / +XP สกิล, ให้ไอเทม / ทอง / XP, และปลดล็อกตรา — token อยู่ในหน่วยความจำ server (รีสตาร์ทแล้วต้อง login ใหม่)

## 🧪 การทดสอบ

```bash
npm run build             # ตรวจว่า compile ผ่าน
npm run test:achievements # ทดสอบเงื่อนไขตราลับทั้ง 15 ตรา (ใช้ DB ชั่วคราว)
npm run test:daily       # ทดสอบระบบ Daily Quest (สุ่ม/รับรางวัล/โบนัส)
npm run test:events      # ทดสอบระบบเหตุการณ์สุ่มระหว่าง session (ทุกชนิด + บันทึก HP/MP ลง log)
npm run test:api         # ทดสอบ API routes end-to-end (รัน server ชั่วคราว + DB แยก: create/equip ข้อจำกัด/complete/session-history/stats/backup/restore/reset)
npm run test:llm         # ทดสอบโมดูล LLM (mock server — ไม่ต้องใช้เครดิตจริง)
# ทดสอบ API ด้วยมือ: รัน `npm start` แล้วใช้ curl ตามตาราง API ด้านบน
```

---

สร้างด้วย 💜 ให้การโฟกัสงานสนุกขึ้นทุกนาที!
